import React from "react";
import Wrapper from "./Wrapper";

const Header = () => {
  return (
    <header className="bg-[var(--black-heavy)] min-h-screen text-white">
      <Wrapper>header</Wrapper>
    </header>
  );
};

export default Header;
